#include "config_parser.hpp"
#include "utils.hpp"  // Creates dependency

void ConfigParser::loadConfig(const char* filename) {
    // Load configuration
    utilityFunction();
}

void ConfigParser::saveConfig(const char* filename) {
    // Save configuration
    utilityFunction();
}
